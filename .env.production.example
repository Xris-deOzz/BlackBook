# Perun's BlackBook - Production Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit your actual .env file to version control!

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_NAME=perunsblackbook
DB_USER=blackbook
# Generate a strong password: openssl rand -base64 32
DB_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# =============================================================================
# APPLICATION SECURITY
# =============================================================================
# Generate a strong secret key: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_TO_A_RANDOM_SECRET_KEY

# Generate an encryption key: openssl rand -base64 32
# This key encrypts sensitive data in the database (OAuth tokens, etc.)
# IMPORTANT: Keep a backup of this key! If lost, encrypted data cannot be recovered.
ENCRYPTION_KEY=CHANGE_ME_TO_A_RANDOM_ENCRYPTION_KEY

# =============================================================================
# GOOGLE OAUTH CONFIGURATION
# =============================================================================
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a project (or use existing)
# 2. Configure OAuth consent screen
# 3. Create OAuth 2.0 Client ID (Web application type)
# 4. Add authorized redirect URI (see GOOGLE_REDIRECT_URI below)

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Redirect URI - update with your actual access URL
# For Tailscale: http://[nas-tailscale-ip]:8000/auth/google/callback
# For domain: https://blackperun.com/auth/google/callback
GOOGLE_REDIRECT_URI=http://YOUR_NAS_IP:8000/auth/google/callback

# =============================================================================
# AI API KEYS
# =============================================================================
# Get from respective provider dashboards

# Anthropic Claude - https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api...

# OpenAI - https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Google Gemini - https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=AIza...

# =============================================================================
# NOTES FOR SYNOLOGY DEPLOYMENT
# =============================================================================
# 1. Create required directories on NAS:
#    sudo mkdir -p /volume1/docker/blackbook/{postgres_data,backups,data}
#
# 2. Set proper permissions:
#    sudo chown -R 1000:1000 /volume1/docker/blackbook
#
# 3. Start containers:
#    docker-compose -f docker-compose.prod.yml up -d --build
#
# 4. Access via:
#    - Local network: http://[nas-local-ip]:8000
#    - Tailscale: http://[nas-tailscale-ip]:8000
#
# 5. Import database (if migrating from Windows):
#    ./scripts/import_database.sh /volume1/docker/blackbook/backups/your_export.sql
