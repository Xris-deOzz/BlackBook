# Perun's BlackBook - Environment Variables Example
# Copy this to .env and fill in your values

# =============================================================================
# DATABASE
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=perunsblackbook
DB_USER=blackbook
DB_PASSWORD=your_password_here

# =============================================================================
# APPLICATION
# =============================================================================
SECRET_KEY=generate-a-random-secret-key-here
DEBUG=true

# =============================================================================
# GOOGLE OAUTH (Phase 3A)
# =============================================================================
# Get these from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
# See docs/GOOGLE_SETUP.md for step-by-step instructions

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Redirect URI must match exactly what's configured in Google Cloud Console
# For local development:
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# For Synology deployment (uncomment and update):
# GOOGLE_REDIRECT_URI=http://your-nas-ip:8000/auth/google/callback

# =============================================================================
# ENCRYPTION (Phase 3A)
# =============================================================================
# Used to encrypt OAuth refresh tokens stored in the database
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# 
# IMPORTANT: 
# - Keep this key secure and backed up
# - If you lose this key, users will need to re-connect their Google accounts
# - Never commit this key to version control

ENCRYPTION_KEY=your-fernet-encryption-key-here

# =============================================================================
# AI PROVIDERS (Phase 5)
# =============================================================================
# Configure AI providers for the Research Assistant feature.
# You can either:
# 1. Set API keys here (environment variables), OR
# 2. Add API keys via Settings > AI Providers in the UI (stored encrypted in DB)
#
# Note: Keys in .env will be used as fallback if no key exists in database

# Default AI provider: "openai", "anthropic", or "google"
AI_DEFAULT_PROVIDER=anthropic

# Maximum tokens to include in AI context (affects cost and response quality)
AI_MAX_CONTEXT_TOKENS=4000

# Enable streaming responses (set to false for simpler debugging)
AI_STREAMING_ENABLED=true

# OpenAI API Key (GPT-4, GPT-3.5, etc.)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic API Key (Claude models)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Google AI API Key (Gemini models)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=
