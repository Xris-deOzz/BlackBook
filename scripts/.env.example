# Perun's BlackBook - Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=perunsblackbook
DB_USER=blackbook
DB_PASSWORD=your_secure_password_here

# Or use a connection string (takes precedence if set)
# DATABASE_URL=postgresql://blackbook:password@localhost:5432/perunsblackbook

# Import Settings
DATA_DIR=../data
LOG_LEVEL=INFO
